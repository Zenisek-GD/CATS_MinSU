# Classroom System Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     CATS Classroom System                        │
└─────────────────────────────────────────────────────────────────┘

┌──────────────┐                                    ┌──────────────┐
│   TEACHER    │                                    │   STUDENT    │
│   Frontend   │                                    │   Frontend   │
└──────┬───────┘                                    └──────┬───────┘
       │                                                   │
       │  Create/Manage                                    │  Join/View
       │  Classrooms                                       │  Resources
       │                                                   │
       ├───────────────────┐                   ┌──────────┤
       │                   │                   │          │
       ▼                   ▼                   ▼          ▼
┌─────────────┐     ┌─────────────┐    ┌─────────────┐  │
│  Teacher    │     │   QR Code   │    │   Student   │  │
│ Controller  │     │  Generator  │    │ Controller  │  │
└──────┬──────┘     └──────┬──────┘    └──────┬──────┘  │
       │                   │                   │          │
       │                   │                   │          │
       └───────────────────┴───────────────────┴──────────┘
                           │
                           ▼
                  ┌────────────────┐
                  │   Classroom    │
                  │     Policy     │
                  │ (Authorization)│
                  └────────┬───────┘
                           │
                           ▼
       ┌───────────────────────────────────────────┐
       │            Database Layer                  │
       ├───────────────────────────────────────────┤
       │  • classrooms                             │
       │  • classroom_students                     │
       │  • classroom_quizzes                      │
       │  • classroom_simulations                  │
       │  • classroom_modules                      │
       │  • users (with teacher/student roles)     │
       └───────────────────────────────────────────┘
```

## Data Flow Diagrams

### Teacher Creates Classroom

```
┌─────────┐
│ Teacher │
└────┬────┘
     │
     │ 1. POST /api/teacher/classrooms
     │    { name, description }
     ▼
┌─────────────────────┐
│ TeacherClassroom    │
│    Controller       │
└─────────┬───────────┘
          │
          │ 2. Create classroom record
          ▼
     ┌─────────┐
     │Classroom│
     │  Model  │
     └────┬────┘
          │
          │ 3. Auto-generate unique code
          │    (e.g., "ABC12345")
          ▼
     ┌─────────┐
     │Database │
     └────┬────┘
          │
          │ 4. Generate QR code
          ▼
     ┌──────────────┐
     │ QR Generator │
     │ (SimpleSoft) │
     └──────┬───────┘
            │
            │ 5. Save QR image
            ▼
       ┌─────────┐
       │ Storage │
       │  /qr-   │
       │ codes/  │
       └─────────┘
```

### Student Joins Classroom

```
┌─────────┐
│ Student │
└────┬────┘
     │
     │ Option A: Scan QR Code
     ├──────────────────────┐
     │                      │
     │ Option B: Enter Code │
     │                      │
     ▼                      ▼
┌──────────┐         ┌──────────┐
│QR Scanner│         │ Manual   │
│(Frontend)│         │  Input   │
└────┬─────┘         └────┬─────┘
     │                    │
     │ Extract code       │
     └──────────┬─────────┘
                │
                │ POST /api/student/classrooms/join
                │ { code: "ABC12345" }
                ▼
       ┌─────────────────────┐
       │ StudentClassroom    │
       │    Controller       │
       └─────────┬───────────┘
                 │
                 │ 1. Verify code exists
                 │ 2. Check classroom active
                 │ 3. Check not already enrolled
                 ▼
            ┌─────────┐
            │Database │
            │ INSERT  │
            │classroom│
            │_students│
            └─────────┘
```

### Teacher Assigns Quiz

```
┌─────────┐
│ Teacher │
└────┬────┘
     │
     │ POST /api/teacher/classrooms/{id}/quizzes
     │ { quiz_id: 5, due_date: "2026-05-15" }
     ▼
┌─────────────────────┐
│ TeacherClassroom    │
│    Controller       │
└─────────┬───────────┘
          │
          │ 1. Verify ownership (Policy)
          │ 2. Verify quiz exists
          ▼
     ┌──────────┐
     │Classroom │
     │  Policy  │
     └────┬─────┘
          │
          │ 3. Create assignment
          ▼
     ┌─────────┐
     │Database │
     │ INSERT  │
     │classroom│
     │_quizzes │
     └─────────┘
          │
          │ 4. Students can now see quiz
          ▼
     ┌─────────┐
     │ Student │
     │Dashboard│
     └─────────┘
```

## Database Relationships

```
┌──────────────────┐
│      users       │
│──────────────────│
│ id               │◄────┐
│ name             │     │
│ email            │     │
│ role (teacher/   │     │
│      student)    │     │
└──────────────────┘     │
         │               │
         │ teacher_id    │ student_id
         │               │
         ▼               │
┌──────────────────┐     │
│   classrooms     │     │
│──────────────────│     │
│ id               │◄────┼────────┐
│ teacher_id       │─────┘        │
│ name             │              │
│ code (unique)    │              │
│ qr_code_path     │              │
│ status           │              │
└──────────────────┘              │
         │                        │
         │ classroom_id           │
         │                        │
         ├────────────────────────┼──────────────┐
         │                        │              │
         ▼                        ▼              ▼
┌──────────────────┐   ┌──────────────────┐   ┌──────────────────┐
│classroom_students│   │classroom_quizzes │   │classroom_modules │
│──────────────────│   │──────────────────│   │──────────────────│
│ classroom_id     │   │ classroom_id     │   │ classroom_id     │
│ student_id       │───┤ quiz_id          │   │ module_id        │
│ joined_at        │   │ due_date         │   │ due_date         │
│ status           │   │ is_active        │   │ is_active        │
└──────────────────┘   └──────────────────┘   └──────────────────┘
                                │
                                │
                       ┌──────────────────────┐
                       │classroom_simulations │
                       │──────────────────────│
                       │ classroom_id         │
                       │ simulation_id        │
                       │ due_date             │
                       │ is_active            │
                       └──────────────────────┘
```

## Authentication & Authorization Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. Login
     │ POST /api/auth/login
     ▼
┌──────────┐
│   Auth   │
│Controller│
└────┬─────┘
     │
     │ 2. Generate JWT token
     ▼
┌──────────┐
│   JWT    │
│  Token   │
└────┬─────┘
     │
     │ 3. Include in requests
     │ Authorization: Bearer {token}
     ▼
┌──────────┐
│  Route   │
│Middleware│
└────┬─────┘
     │
     │ 4. Verify token
     │ 5. Check role
     ▼
┌──────────┐
│RequireRole
│Middleware│
└────┬─────┘
     │
     │ 6. Role matches?
     ├─── Yes ──────┐
     │              │
     └─── No ───┐   │
                │   │
                ▼   ▼
           ┌────────────┐
           │  403       │
           │ Forbidden  │
           └────────────┘
                    │
                    │ 7. Check ownership
                    ▼
               ┌──────────┐
               │Classroom │
               │  Policy  │
               └────┬─────┘
                    │
                    │ 8. Authorized?
                    ├─── Yes ──────┐
                    │              │
                    └─── No ───┐   │
                               │   │
                               ▼   ▼
                          ┌────────────┐
                          │  403       │
                          │ Forbidden  │
                          └────────────┘
                                   │
                                   │ 9. Execute action
                                   ▼
                              ┌──────────┐
                              │Controller│
                              │  Method  │
                              └──────────┘
```

## QR Code Generation Process

```
┌──────────────────┐
│ Classroom Created│
└────────┬─────────┘
         │
         │ 1. Generate unique code
         │    Code: "ABC12345"
         ▼
    ┌─────────┐
    │Classroom│
    │  Model  │
    └────┬────┘
         │
         │ 2. Build join URL
         │    URL: "http://localhost:3000/join-classroom/ABC12345"
         ▼
    ┌──────────────┐
    │ QR Generator │
    │(SimpleSoft)  │
    └──────┬───────┘
           │
           │ 3. Generate PNG image
           │    Size: 300x300
           │    Format: PNG
           ▼
      ┌─────────┐
      │ Storage │
      │  Save   │
      └────┬────┘
           │
           │ 4. Save path to database
           │    Path: "qr-codes/classroom-1-ABC12345.png"
           ▼
      ┌─────────┐
      │Database │
      │ UPDATE  │
      └────┬────┘
           │
           │ 5. Return QR URL
           │    URL: "/storage/qr-codes/classroom-1-ABC12345.png"
           ▼
      ┌─────────┐
      │ Teacher │
      │Dashboard│
      └─────────┘
```

## Resource Assignment Flow

```
┌─────────┐
│ Teacher │
└────┬────┘
     │
     │ Assign Quiz/Simulation/Module
     ▼
┌──────────────────┐
│   Controller     │
│ (Authorization)  │
└────────┬─────────┘
         │
         │ Create pivot record
         ▼
┌──────────────────┐
│  classroom_*     │
│  (pivot table)   │
└────────┬─────────┘
         │
         │ Students query
         ▼
┌──────────────────┐
│ Student sees     │
│ assigned resource│
└──────────────────┘
         │
         │ Student completes
         ▼
┌──────────────────┐
│ Progress tracked │
│ in existing      │
│ tables           │
└──────────────────┘
         │
         │ Teacher views
         ▼
┌──────────────────┐
│   Analytics      │
│   Dashboard      │
└──────────────────┘
```

## API Request/Response Flow

```
┌──────────┐
│ Frontend │
└────┬─────┘
     │
     │ HTTP Request
     │ Headers: Authorization, Content-Type
     │ Body: JSON data
     ▼
┌──────────┐
│  Route   │
│  Layer   │
└────┬─────┘
     │
     │ Match route
     ▼
┌──────────┐
│Middleware│
│  Stack   │
└────┬─────┘
     │
     │ 1. CORS
     │ 2. JWT Auth
     │ 3. Role Check
     ▼
┌──────────┐
│Controller│
└────┬─────┘
     │
     │ 1. Validate input
     │ 2. Check authorization
     │ 3. Business logic
     ▼
┌──────────┐
│  Model   │
└────┬─────┘
     │
     │ Database query
     ▼
┌──────────┐
│ Database │
└────┬─────┘
     │
     │ Return data
     ▼
┌──────────┐
│Controller│
└────┬─────┘
     │
     │ Format response
     ▼
┌──────────┐
│   JSON   │
│ Response │
└────┬─────┘
     │
     │ HTTP Response
     │ Status: 200/201/400/403/404
     │ Body: JSON data
     ▼
┌──────────┐
│ Frontend │
└──────────┘
```

## Component Architecture

```
Backend (Laravel)
├── Routes (api.php)
│   ├── Teacher routes (/api/teacher/classrooms/*)
│   └── Student routes (/api/student/classrooms/*)
│
├── Middleware
│   ├── JwtAuth (verify token)
│   └── RequireRole (check role)
│
├── Controllers
│   ├── TeacherClassroomController
│   │   ├── CRUD operations
│   │   ├── QR code management
│   │   ├── Student management
│   │   └── Resource assignment
│   │
│   └── StudentClassroomController
│       ├── Join classroom
│       ├── View resources
│       └── Leave classroom
│
├── Policies
│   └── ClassroomPolicy (authorization)
│
├── Models
│   ├── Classroom
│   ├── ClassroomStudent
│   ├── ClassroomQuiz
│   ├── ClassroomSimulation
│   └── ClassroomModule
│
└── Database
    ├── Migrations (schema)
    └── Seeders (test data)

Frontend (React/Vue/Angular)
├── Pages
│   ├── Teacher Dashboard
│   │   ├── Classroom List
│   │   ├── Create Classroom
│   │   ├── Classroom Details
│   │   └── Analytics
│   │
│   └── Student Dashboard
│       ├── Join Classroom
│       ├── My Classrooms
│       └── Classroom Resources
│
├── Components
│   ├── QR Scanner
│   ├── QR Code Display
│   ├── Classroom Card
│   ├── Resource List
│   └── Student List
│
└── Services
    ├── API Client
    ├── Auth Service
    └── Classroom Service
```

## Security Layers

```
┌─────────────────────────────────────┐
│         Request from Client         │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Layer 1: CORS Middleware           │
│  - Verify origin                    │
│  - Check allowed methods            │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Layer 2: JWT Authentication        │
│  - Verify token signature           │
│  - Check token expiration           │
│  - Extract user info                │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Layer 3: Role-Based Access         │
│  - Check user role                  │
│  - Verify role matches route        │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Layer 4: Resource Authorization    │
│  - Check ownership (Policy)         │
│  - Verify permissions               │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  Layer 5: Input Validation          │
│  - Validate request data            │
│  - Sanitize inputs                  │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│      Execute Business Logic         │
└─────────────────────────────────────┘
```

## Deployment Architecture

```
┌─────────────────────────────────────────────────┐
│              Production Environment              │
└─────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐
│   Frontend   │         │   Backend    │
│   (React)    │◄───────►│  (Laravel)   │
│              │   API   │              │
│ Port: 3000   │         │ Port: 8000   │
└──────────────┘         └──────┬───────┘
                                │
                                │
                    ┌───────────┴───────────┐
                    │                       │
                    ▼                       ▼
            ┌──────────────┐        ┌──────────────┐
            │   Database   │        │   Storage    │
            │   (MySQL)    │        │  (QR Codes)  │
            │              │        │              │
            │ Port: 3306   │        │ /storage/    │
            └──────────────┘        └──────────────┘
```

---

This architecture provides:
- ✅ Clear separation of concerns
- ✅ Role-based access control
- ✅ Secure authentication
- ✅ Scalable design
- ✅ Easy to maintain
- ✅ Well-documented flows
